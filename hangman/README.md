## Hangman

This project selects a random word and hides its letters with underscores. The user is prompted to guess one letter at a time, and the program validates each input before checking whether the letter is in the word. It also displays the remaining lives and the letters that are still missing. The game continues until the user guesses the full word or runs out of lives.
