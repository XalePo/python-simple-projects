## Password Generator

This project prompts the user to enter a password length between 8 and 20 characters. It then uses Python’s `secrets` and `string` modules to generate a random password based on the length entered by the user.
