## Rock, Paper, Scissors

This program prompts the user to choose either rock, paper, or scissors, and then compares that choice to the computer’s option. Using `if`/`elif`/`else` statements, it determines the winner of each round. The user is then prompted to play again, and the program keeps track of the scores.
