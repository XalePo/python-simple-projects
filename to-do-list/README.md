## Terminal To-Do List

This program keeps track of the user’s tasks and stores them on disk for future use. The user can view tasks, add a new task, or delete an existing one by selecting from the menu options. When the user chooses to exit, all changes are saved to `tasks.txt`. If the file does not exist, the program starts with an empty task list and creates the file when the tasks are saved.
